# ==========================================
# Lume  - Dependencies
# ==========================================

# OCR Engine
marker-pdf>=0.3.0

# PDF Manipulation (Cover and Content Extraction)
PyMuPDF>=1.23.0

# HTML Cleaning and Reconstruction (Healer)
beautifulsoup4>=4.12.0

# Communication with the Pandoc compiler
pypandoc>=1.12

# NOTE ON PYTORCH (CUDA):
# PyTorch is intentionally omitted from this list to prevent
# 'pip' from installing the default CPU version. 
# For NVIDIA GPU support, install PyTorch manually using:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
